// Ось импорта по работодателю — рядом с OCCUPATIONS (src/lib/occupations.ts),
// не внутри src/lib/importers/jobtech.ts: список подрядчиков и регион-
// приоритет это конфигурация, а не логика импортёра.
//
// Зачем вообще эта ось: проверено на реальном API 16.08.2026 — ось по
// occupation-термину слабее оси по работодателю. N Kraft Bemanning AB
// (кадровое агентство, возит бригады на Luleå/Boden) отдаёт 1547 вакансий
// по всей Швеции, но только четверть их заголовков попадает под термины
// поиска ("Helpers", "Dumperchaufförer" и т.п. — синонимов не напасёшься).
// Проще искать по имени известного подрядчика напрямую.
//
// Stegra и Boliden сознательно НЕ в списке, хотя географически сидят в
// целевом регионе сильнее многих: по факту публикуют в основном офисные и
// управленческие роли (Treasury Analyst, Head of Internal Audit,
// Sektionschef) — синих воротничков на своих мегапроектах нанимают через
// подрядчиков/кадровые агентства вроде тех, что ниже, не публикуют вакансии
// от своего имени. Добавление одного из них в whitelist искало бы не ту
// популяцию, которую нашла проверка.
export const CONTRACTOR_EMPLOYERS: readonly string[] = [
  "N Kraft Bemanning",
  "ExpanderaMera",
  "MW Sverige",
  "MW Field Services",
  "NCC",
];

// JobTech region= (Swedish län code). Подтверждено вживую 16.08.2026:
// region=25 -> Norrbottens län, region=24 -> Västerbottens län, несколько
// значений параметра работают как OR (region=25&region=24 дало ровно сумму
// отдельных запросов, без потерь и дублей).
//
// Обязательно применяется вместе с осью по работодателю (см. выше) — без
// региона один подрядчик вроде N Kraft залил бы сайт вакансиями по всей
// Швеции, не только по целевому северному сегменту. Тем же регионом
// ограничены и новые occupation-термины (Occupation.regionRestricted в
// occupations.ts) — они добавлены в список именно за концентрацию в этих
// двух län, не как общесоюзное расширение поиска.
//
// НЕ жёсткое исключение навсегда: решение принято по данным северного
// сегмента (дата-центры/промышленные проекты под Боденом), подлежит
// пересмотру, когда дойдут руки до других регионов (Стокгольм, Гётеборг
// и т.п.) — тогда здесь добавляется ещё один код, не переписывается
// логика импортёра.
export const PRIORITY_REGIONS: readonly string[] = ["25", "24"];
